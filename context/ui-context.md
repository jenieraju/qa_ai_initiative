# UI Context

_Generated by get-ui-context on 2026-08-13. Re-run when the app or test setup changes._

## Doc sources

- Repo: `cofee-web` (https://github.com/KeyValueSoftwareSystems/cofee-web) — shallow-cloned to a scratch location, used as repo root
- context/ location: project root of this automation project (`sara-trial-1`), a sibling of the `cofee-web` clone, never inside it
- Primary (UI shape): routing definitions (`src/constants/routes.ts`, `src/features/lead-management/routes.tsx`) — tier 1 of the discovery order
- Other sources seen (not used as primary): `LMS.pdf` (Figma export, supplied directly as a PDF — no live Figma link/MCP available)
- Chat-provided content saved this run: none (the PDF was supplied as a local file path, not pasted/uploaded inline — referenced directly rather than re-saved as a `context/figma-*.png`)
- See also: `context/business-context.md` (this skill's `## UI Automation` section) for business rules, entities, and PRD/Figma/Jira-derived intent — not duplicated here.

## Page / route inventory

Scope: the 5 screens shown in `LMS.pdf` (Dashboard, Leads list, Leads analytics, Agents, Sources), all under the Lead Management (LMS) feature. `LMS.pdf`'s "Leads analytics" section is a **tab** on the same route as "Leads list", not a separate page — confirmed in code (see Business rules below).

| Route | Page / component | Purpose | Key components | Test-id(s) / locator strategy | Auth required | Confirmed |
|-------|-------------------|---------|-----------------|-------------------------------|----------------|-----------|
| `/lead-management/dashboard` | `LeadManagementDashboard.tsx` | LMS-specific dashboard — greeting, stuck/overdue/new/unassigned lead counts, conversion funnel, revenue-at-risk/lost, conversion rate, avg time to close | date-range picker (This Month/Last Month/Custom), 4 metric cards, conversion funnel chart | No `data-testid` found — role+name / text-based locators only (matches this codebase's known near-zero test-id coverage) | Session cookie (OTP login) | inferred from code + visual-confirmed via `LMS.pdf` |
| `/lead-management/list` (List tab) | `LeadList.tsx` → `LeadTable.tsx` | Leads table — name, mobile, tags, source, assignee, days stuck, status, actions; search, filter, Add Lead | search bar, filter button, "+ Add Lead" button (permission-gated), data table, pagination | No `data-testid` found | Session cookie | inferred from code + visual-confirmed via `LMS.pdf` |
| `/lead-management/list` (Analytics tab) | `LeadList.tsx` → `LeadAnalytics.tsx` (component, not a separate route) | Leads analytics — leads by top locations, leads by source (donut), lead count & revenue by status (chart), leads distribution by agent (table) | date-range picker (This Month/Last Month/Custom), 2 charts, 1 combo chart, 1 table with Count/Revenue toggle | No `data-testid` found | Session cookie | inferred from code + visual-confirmed via `LMS.pdf` |
| `/lead-management/agents` | `LeadAgents.tsx` | Agent performance — team avg conversion, team avg conversion time, agents above/below average, per-agent table (leads/converted/conflicted/overdue/avg conversion time/performance) | date-range picker, 4 metric cards, data table with search/filter | No `data-testid` found | Session cookie | inferred from code + visual-confirmed via `LMS.pdf` |
| `/lead-management/sources` | `LeadSources.tsx` | Lead sources — top volume/highest conversion/highest revenue cards, distribution-by-source table, link to Ad sources | date-range picker, 3 metric cards, table, "View Ad Sources" link | No `data-testid` found | Session cookie | inferred from code + visual-confirmed via `LMS.pdf` |
| `/lead-management/sources/ad-sources` | `LeadAdSources.tsx` | Ad-specific breakdown of sources — same shape as Sources, scoped to ad campaigns | date-range picker, 3 metric cards, distribution-by-ad table | No `data-testid` found | Session cookie | inferred from code + visual-confirmed via `LMS.pdf` |

**Adjacent LMS routes seen in code but not in scope for this pass** (not shown in `LMS.pdf`): `LEAD_ADD` (`/lead-management/add`), `LEAD_EDIT`, `LEAD_DETAILS`, `LEAD_ACTIVITIES`, `LEAD_SETTINGS`, `LEAD_REPORTS` (a distinct report-generation/download page, permission-gated by `lead_report_generate` — not the "Leads analytics" tab, despite the similar name).

## Existing UI test/framework setup

- Framework: none yet in *this* project — just scaffolded via `create-ui-framework-structure` this session (Playwright Python, `@playwright/test`/`playwright` version not yet pinned — no lockfile exists yet)
- Test layout: `tests/<feature>/`, page objects under `src/pages/` — both currently empty, per this session's scaffold
- Config/fixtures: base URL via `config/config.yaml` + `src/config/config_loader.py`; `--env` option in root `conftest.py`; auth fixtures not yet added (pending `get-ui-auth`)
- Locator contract: **no `data-testid` convention found anywhere in the 6 files checked** — role+name / label / text / id are the primary, expected locators for this app (matches this codebase's known-sparse test-id coverage, consistent with the API-side `get-context`'s own prior finding for cofee-web)
- Gaps noted: no existing coverage for any LMS screen yet — this is the first feature being automated in this framework

## Open questions / follow-ups

- **Exact selector strings weren't individually catalogued this pass** (button text, table column headers, chart labels) beyond what's visible in `LMS.pdf`'s screenshots and the component imports found in code — `ui-test-automation` should do a final locator confirmation (live-inspected DOM, with permission) before generating tests, per its own discovery order.
- **`LEAD_MANAGEMENT_CREATE` permission gates the "Add Lead" button** on the List tab (inferred from `LeadList.tsx:86-88`) — worth an `auth-session` case per role (e.g. does a Sales Agent without this permission see the page at all, just without the button, or is the whole page blocked?) — not fully traced this pass.
- **Whether any of these 5 pages are page-level permission-gated** (like `/lead-management/reports` is, by `LEAD_REPORT_GENERATE`) wasn't confirmed for Dashboard/List/Agents/Sources specifically — only the Reports page and the Add-Lead button were found to have explicit permission checks in the files read. Flag as unresolved rather than assuming all 5 are open to every logged-in role.
- **"Stuck lead" / "overdue lead" threshold definitions** (e.g. how many days without activity counts as "stuck") aren't stated in `LMS.pdf` or the code read this pass — needed for a precise `boundary` case later; flagging rather than guessing a number.
- **Whether this project has an accessibility or visual-regression tool wired in** — neither exists yet (greenfield framework), so `visual-a11y` cases will need that gap flagged when `ui-test-design` runs.
