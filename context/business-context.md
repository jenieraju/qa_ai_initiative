# Business Context

_Shared across automation tracks. Each track owns and rewrites only its own marked section below — see that section's header for which skill owns it._

<!-- ui-automation:start -->
## UI Automation (from get-ui-context)

_Generated by get-ui-context on 2026-08-24. This section only — the rest of this file belongs to other tracks (e.g. get-context) and is left untouched._

### Repo-derived signal
_Source: repo not available — skipped._

- No repo was connected/available this run, so no README/docs/code-comment/validation-logic signal could be mined.

### Figma
_Source: uploaded PDF export `context/figma-lms-v2.pdf` ("LMS V2 (2).pdf"), visual-reference-only — not an MCP-linked Figma file, so no tokens/variables/breakpoint specs are available. **Confirmed byte-identical to the Figma export used in an earlier automation run against this same app — not a distinct revamp design file** (user directed proceeding with it anyway for this "LMS revamp" task)._

- **Screens/states covered:** Dashboard (populated default, stuck-leads-detail variant with tiered thresholds, empty state), Leads list (populated), Leads analytics (two populated variants — percentage-axis and revenue-axis conversion views — plus empty state), Agents (populated, empty state), Sources (populated, Ad Sources drill-in, empty state), plus a one-page sitemap index of all the above.
- **Layout / component / copy notes:**
  - App name "CoFee"; persistent left sidebar nav across every screen (Organization dashboard, Reports, Expense tracker, Lead management [Dashboard, Leads, Agents, Sources, Reports], Settings [Configurations, Team management, Plan & Billing], Help [Docs, Sandbox], Log out).
  - Dashboard greets the logged-in user by name ("Hello John, here's what needs your attention") — every screen assumes an authenticated session, though no login screen itself is in this export.
  - Consistent date-range control across Dashboard/Leads Analytics/Agents/Sources: "This Month / Last Month / Custom" (labeled "Select range" in some variants).
  - Every list/chart-heavy page (Dashboard, Leads Analytics, Agents, Sources) has a matching empty-state variant reading "No Leads Data Available — Looks like you haven't added any data yet." or "No data to show!" — empty states are a deliberate, recurring design pattern, not a one-off.
  - Leads list and Leads Analytics share a page via a "List / Analytics" toggle rather than being fully separate routes (per the Figma layout) — noted here since it affects how `ui-test-design` should scope test cases (same page, two views) rather than two unrelated pages.
- **Business rules / edge cases (stated in Figma annotation, not inferred from code):**
  - **Stuck Leads escalation tiers:** leads are flagged "stuck" at 5+, 8+, 10+, 12+, and 15+ days without progress (visible as tiered thresholds on the Dashboard's Stuck Leads / Overdue Leads detail variant).
  - **Custom source icon rule:** when a user adds a new lead source not in the built-in list, it gets either (a) an initials badge — the source name's first letter(s) in a colored circle (e.g. "School Fair" → "SF"), confirmed visually by sources like "RF Referral partner", "TF Trade Fair", "TC TV Campaign" in the populated Sources screen — or (b) a single fixed icon (e.g. a speakerphone) shared by every custom source, for visual consistency. Both options are shown as annotated alternatives in the design file; which one ships is not resolved by this export alone.

### PRD
_Source: no PRD found — none in `artifacts/`, none pasted/linked in conversation this run._

### Documents (other than the PRD)
_Source: none._

### Jira tickets
- None shared.

### Cross-track notes
- `context/business-context.md` did not exist on this branch before this run — created fresh by `get-ui-context`. No API-track section exists yet to reconcile against.
- **The actual LMS revamp was found via live reconciliation, not the Figma:** the lead status/stage taxonomy has been unified into one 10-value enum (`Prospect, Contacted, Market Qualified, Parked, Sales Qualified, Interested, In Discussion, Onboarded, Won, Lost`) used consistently across the Dashboard's Conversion Funnel, the Leads list Status column, Leads Analytics, Agents distribution, and the Reports filter — where the Figma-era Dashboard funnel and Leads list Status column each used their own separate, different vocabulary. See `context/ui-context.md`'s "⚠ The revamp" section for full detail. Treat this as the authoritative business-rule change for this automation task; the Figma-derived rules above (stuck-lead tiers, custom-source-icon rule) were not observed to have changed and remain as stated.
<!-- ui-automation:end -->
