[pytest]
addopts = -s --alluredir=reports/allure-results
markers =
    smoke: fastest, smallest "is the build alive" set
    sanity: primary happy-path checks
    regression: full suite
    p0: critical priority
    p1: high priority
